name: noled
description: "NoLED notification overlay for AMOLED screens"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.0
  device_apps: ^2.2.0
  battery_plus: ^6.0.0 # Reads live charging status and levels

flutter:
  uses-material-design: true
